Sources for the plugin `plugin.google.iap.v3`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.google.iap.v3" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
